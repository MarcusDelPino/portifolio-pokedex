/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['source.unsplash.com', 'images4.alphacoders.com', 'raw.githubusercontent.com', 'www.hqzona.com.br']
  }
  
};

export default nextConfig;
